🐳 Docker Compose Commands
1️⃣ docker compose up

👉 Starts all services defined in docker-compose.yml

Syntax:

docker compose up

Example:

docker compose up

👉 Runs containers together2️⃣ docker compose up -d

👉 Runs services in background (detached mode)

docker compose up -d


